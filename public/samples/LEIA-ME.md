# Amostras do modo demo

- `demo-clip.mp4` — vídeo curto (ideal 9:16, ~5–15s, leve/baixa resolução pra não
  pesar no git) usado como **vídeo de exemplo** do "Em lote" na conta demo. A API
  copia ele + a moldura `templates/demo-randomlyy.png` pro job quando o demo gera
  o exemplo.

Mantenha o arquivo pequeno (alguns MB) — ele vai no git e é só pra demonstração.
